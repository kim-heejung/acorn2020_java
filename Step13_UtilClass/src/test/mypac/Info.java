package test.mypac;

public class Info {
	//필드
	public int num;
	public String name;
	public String addr;
	/* 
	 * filed, constructor, method, class 별 사용가능 접근 지정자.
	 * filed : public, protected, X (default), private.
	 * - . 찍어서 참조 할 수 있느냐.
	 * constructor : public, protected, X (default), private.
	 * -new 를 할 수 있느냐.
	 * method : public, protected, X (default), private.
	 * - . 찍어서 호출 할 수 있느냐.
	 * class : public, X (default)
	 * -import 가능 여부.
	 */
}
